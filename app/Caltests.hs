module Caltests where

import Events
import Common
import Datetests
import Data.Time


-- vevents = [Vevent (DT UTCTime (fg 2014 12 12) (stdt 33400)) (UID " ") PUBLIC (DateStart $ UTCTime (fg 2014 12 12) (stdt 33400)) (DateStop $ Just $ UTCTime (fg 2014 12 12) (stdt 36400)) 
--                    (Duration Nothing) (Desc $ Just "Event") (Priority (Just 3)) (EvtSequence Nothing) (Just TRANSPARENT) Nothing,
--                    Vevent (DT UTCTime (fg 2014 12 12) (stdt 72100) ) (UID " ") PUBLIC (DateStart $ UTCTime (fg 2014 12 12) (stdt 72100)) (DateStop $ Just UTCTime (fg 2014 12 12) (stdt 75700)) 
--                    (Duration Nothing) (Desc $ Just (Desc $ Just "Event1")) (Priority (Just 3)) (EvtSequence Nothing) (Just TRANSPARENT) Nothing       
--           ]

